Whenever you're doing frontend changes, make sure that you have browser tests covering the functionality, we dont want things to fail due to an element not rendering or something like that.

Not just SmokeTests, even though they are good to have as well to make sure the page loads without errors, but actual functionality tests. Feature tests.
