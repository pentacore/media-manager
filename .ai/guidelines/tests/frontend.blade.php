# Frontend Browser Testing

Whenever making frontend changes, add browser tests that cover the changed functionality. Smoke tests are useful for proving a page loads without errors, but they do not replace functional browser tests that exercise the affected user flow and confirm the expected elements render.
