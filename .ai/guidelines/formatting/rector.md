before formatting with pint, always use `vendor/bin/sail bin rector` to format and clean up the code first
Also remember to run `vendor/bin/sail npm run format` to format the frontend code as well, and `vendor/bin/sail npm run lint` to check for any linting issues. This will ensure that the code is clean and well-formatted before applying pint for final touches.
